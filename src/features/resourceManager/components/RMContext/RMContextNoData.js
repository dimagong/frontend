import React from "react";

import RMContextTemplate from "./RMContextTemplate";

const RMContextNoData = () => {
  return (
    <RMContextTemplate>
      <strong className="font-size-large">There is no resources</strong>
    </RMContextTemplate>
  );
};

export default RMContextNoData;
